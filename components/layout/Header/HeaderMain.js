import styled from 'styled-components';

const HeaderMainWrapper = styled.div``;

const HeaderMain = () => {
	return (
		<HeaderMainWrapper>
			HeaderMain
		</HeaderMainWrapper>
	);
};

export default HeaderMain;
