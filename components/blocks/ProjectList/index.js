import styled from 'styled-components';

const ProjectListWrapper = styled.div`
	grid-column: 1 / 2;
`;

const ProjectList = () => {
	return (
		<ProjectListWrapper>
			ProjectList
		</ProjectListWrapper>
	);
};

export default ProjectList;
